<p id="notice"><%= notice %></p>

<h1>Listing Blocks</h1>

<table>
  <thead>
    <tr>
      <th>Tipo</th>
      <th>Cu</th>
      <th>Q</th>
      <th>Mensilita</th>
      <th>Tot</th>
      <th>Attivazione</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @blocks.each do |block| %>
      <tr>
        <td><%= block.tipo %></td>
        <td><%= block.cu %></td>
        <td><%= block.q %></td>
        <td><%= block.mensilita %></td>
        <td><%= block.tot %></td>
        <td><%= block.attivazione %></td>
        <td><%= link_to 'Show', block %></td>
        <td><%= link_to 'Edit', edit_block_path(block) %></td>
        <td><%= link_to 'Destroy', block, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Block', new_block_path %>
